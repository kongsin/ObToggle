<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.obview.kognsin.customtoggle.MainActivity">

    <com.obview.obtoggle.ObToggle
        android:checked="true"
        android:enabled="true"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:textOn="@string/buffet"
        android:textOff="@string/a_la_cart"
        android:id="@+id/obToggle"
        android:textSize="20sp"
        app:leftIconBackgroundDrawable="@drawable/icon_bg"
        app:leftIconDrawable="@drawable/ic_directions_grey"
        app:backgroundDrawable="@drawable/bg"
        />

</RelativeLayout>
